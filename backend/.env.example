# Copy this file to `backend/.env` and fill in your real key.
# `backend/.env` is gitignored — never commit the real key.
# Get a free key at https://sportsapipro.com/register
SPORTSAPIPRO_KEY=your_key_here
